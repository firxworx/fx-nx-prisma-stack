import type { NextPage } from 'next'

export const AppSettingsPage: NextPage = (_props) => {
  return (
    <div>
      <h2 className="text-lg mt-8">Settings Page</h2>
    </div>
  )
}

export default AppSettingsPage
